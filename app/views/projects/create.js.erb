var html = '<div class="col-xs-6 well-box" id="project-<%= @project.id %>"> \
		          <%= link_to @project, remote: true do %> \
                <div class="well selected-<%= @project.id %>"> \
                 <p class="title"><%= @project.title.titleize %> \
                    <%= link_to @project, :method => :delete, remote: true do %> \
                      <i class="fa fa-trash-o"></i> \
                    <% end %> \
                 </p> \
                 <div class="info"> \
                   <p>tasks: <span class="task-count-<%= @project.id %>"><%=@project.tasks.count%></span></p> \
                   <p>in progress:</p> \
                   <p>completed:</p> \
                 </div> \
                </div> \
              <% end %> \
            </div>';

$('#projects_column').append(html);
$('#form-control-project-title').val('').blur();
