var html = '<div class="task-box"> \
              <p><%= @task.title %></p> \
            </div>';

$('#project_details').append(html);
$('.task-count-<%= @project.id %>').html("<%= @project.tasks.count %>");
$('#new-task').val('').blur();